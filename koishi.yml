port: 5140
maxPort: 5149

plugins:
  help:
  suggest:
  console:
    open: true
  logger:
  manager:
    searchUrl: ${{ env.GITHUB_CONTENT_MIRROR }}/koishijs/registry/dist/index.json
  status:
  sandbox:
