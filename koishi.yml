port: 5140
maxPort: 5149

plugins:
  group:basic:
    help:
    suggest:
    rate-limit:
  group:console:
    console:
      open: true
    logger:
    insight:
    market:
      search:
        endpoint: ${{ env.GITHUB_CONTENT_MIRROR }}/koishijs/registry/dist/index.json
    sandbox:
    status:
